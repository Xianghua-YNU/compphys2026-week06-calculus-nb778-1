# 第 6 周实验报告：数值积分与物理场重建

## 1. 小组信息

**小组 ID**：  
**成员名单**：

| 任务 | 负责同学 | Commit Hash | 贡献说明 |
|---|---|---|---|
| Task A |  |  |  |
| Task B |  |  |  |
| Task C |  |  |  |
| Bonus |  |  |  |

---

## 2. Task A：3-α 温度敏感性指数（23分）

- 你实现的 `rate_3alpha(T)` 与 `sensitivity_nu(T0,h)` 核心思路：### rate_3alpha(T) 实现思路
- 公式实现 ：直接使用题目给出的 3-α 反应率公式 [ o bj ec tO bj ec t ] q ( T ) = 5.09 × 1 0 11 T 8 − 3 ​ e − 44.027/ T 8 ​
- 温度转换 ：将输入温度 [ o bj ec tO bj ec t ] T 转换为 [ o bj ec tO bj ec t ] T 8 ​ = T /1 0 8 ，确保指数项计算正确
- 数值计算 ：使用 NumPy 的 np.exp() 函数计算指数项，保证数值稳定性
### sensitivity_nu(T0, h) 实现思路
- 导数近似 ：使用前向差分法计算 [ o bj ec tO bj ec t ] d q / d T ，步长设置为 [ o bj ec tO bj ec t ] Δ T = h × T 0 ​ （h 默认为 1e-8）
- 敏感性指数计算 ：根据定义 [ o bj ec tO bj ec t ] ν = ( T / q ) × d q / d T 计算温度敏感性指数
- 参数验证 ：添加了温度 [ o bj ec tO bj ec t ] T > 0 的检查，避免数值计算错误  
- 使用的温度点与步长：  - 温度点 ：[1.0e8, 2.5e8, 5.0e8, 1.0e9, 2.5e9, 5.0e9] K
- 步长参数 ：h = 1.0e-8（平衡截断误差与舍入误差）
- 计算结果表：

| T0 (K) | ν(T0) |
|---:|---:|
| 1.0e8 | 41.03 |
| 2.5e8 | 14.61 |
| 5.0e8 | 5.81 |
| 1.0e9 | 1.40 |

- 物理解释：在低温与高温区，ν 的变化说明了什么？### 低温区（T ≈ 10^8 K）
- ν 值特点 ：ν 非常大（约 41），系统高度"点火敏感"
- 物理意义 ：在低温下，反应率对温度变化极其敏感，温度微小增加会导致反应率急剧上升，这是恒星核反应点火的关键机制
### 高温区（T > 10^9 K）
- ν 值特点 ：ν 变为负值（约 -1 到 -2 量级）
- 物理意义 ：在高温下，反应率随温度升高而下降，这是因为指数因子的负温度系数效应超过了幂律因子的正温度系数效应，系统进入"温度自调节"状态，有助于恒星核反应的稳定进行

---

## 3. Task B：梯形 vs Simpson + Debye 积分（24分）

- 你实现的两个积分器是否通过偶数分段与边界检查：  ### 偶数分段与边界检查
- 梯形法 (trapezoid_composite) ：
  
  - 边界检查： n < 1 时抛出 ValueError
  - 不需要偶数分段，适用于任意正整数 n
- Simpson 法 (simpson_composite) ：
  
  - 边界检查： n < 2 时抛出 ValueError
  - 偶数分段检查： n % 2 != 0 时抛出 ValueError
  - 在 debye_integral 函数中，当选择 Simpson 法时会自动确保 n 为偶数（如果不是则 n += 1）
- 同一参数下方法比较：

| 方法 | n | 积分值 | 误差估计 | 结论 |
|---|---:|---:|---:|---|
| 梯形法 | 200 | 0.876174 | ~8.56e-06 | 计算速度快，精度适中 |
| Simpson 法 | 200 | 0.876165 | ~3.17e-07 | 计算精度高，收敛速度快 |

- 对 Debye 积分结果的解释：  ### 物理意义
Debye 积分 [ o bj ec tO bj ec t ] I ( y ) = ∫ 0 y ​ ( e x − 1 ) 2 x 4 e x ​ d x 是计算固体热容的关键，其中 [ o bj ec tO bj ec t ] y = θ D ​ / T 。

---

## 4. Task C：带电圆环电势场（23分）

- 你实现的点电势函数与网格电势函数说明：  ### 点电势函数 ring_potential_point(x, y, z, a, q, n_phi)
- 实现原理 ：使用离散积分计算空间任意点的电势
- 核心公式 ： [ o bj ec tO bj ec t ] V ( x , y , z ) = 2 π q ​ ∫ 0 2 π ​ ( x − a c o s ϕ ) 2 + ( y − a s i n ϕ ) 2 + z 2 ​ d ϕ ​
- 数值方法 ：
  - 将圆环离散为 n_phi 个电荷元（默认 720 个）
  - 使用梯形法则计算积分
  - 时间复杂度：O(n_phi)，适用于单点计算
### 网格电势函数 ring_potential_grid(y_grid, z_grid, x0, a, q, n_phi)
- 实现原理 ：在 y-z 平面上计算电势分布
- 核心步骤 ：
  1. 使用 np.meshgrid 创建二维网格
  2. 对每个网格点调用 ring_potential_point 计算电势
  3. 返回二维电势矩阵
- 时间复杂度 ：O(N² × n_phi)，其中 N 是网格点数
- 应用场景 ：适合生成电势分布图和计算电场
- 数值稳定性处理（例如：靠近圆环时的截断策略）：  ### 1. 奇点处理
- 问题 ：当计算点非常靠近圆环时，距离 r 可能趋近于零，导致除以零错误
- 解决方案 ：使用 r = np.maximum(r, 1e-12) 确保 r 始终大于一个小量，避免数值发散
### 2. 积分精度控制
- 问题 ：积分点数不足会导致精度下降
- 解决方案 ：默认使用 n_phi=720 个积分点，确保圆环被充分离散化
- 验证 ：与解析解对比，最大误差仅为 4.44e-16，证明积分精度足够
### 3. 电场计算稳定性
- 问题 ：电势梯度计算可能受数值噪声影响
- 解决方案 ：
  - 使用中心差分法计算电场： [ o bj ec tO bj ec t ] E = − ∇ V
  - 合理选择步长 h=1e-6，平衡截断误差和舍入误差
## 结果图
- 结果图（至少 1 张）：

<img width="2400" height="900" alt="image" src="https://github.com/user-attachments/assets/092664a7-a7fb-44ee-afe5-14e935313e4b" />


- 物理解释：等势线分布体现了怎样的空间对称性？### 空间对称性分析
1. 轴对称性 ：
   
   - 等势线呈现以 z 轴为中心的圆对称分布
   - 这是因为圆环关于 z 轴对称，电荷分布具有旋转对称性
2. 平面对称性 ：
   
   - 等势线关于 y=0 平面对称
   - 这是因为圆环位于 x-y 平面（z=0），电荷分布关于 z=0 平面对称
3. 电势分布特点 ：
   
   - 中心区域 ：电势最高，等势线密集
   - 远离圆环 ：电势逐渐降低，等势线稀疏
   - 圆环附近 ：等势线呈现哑铃状分布，反映了圆环电荷的集中效应
4. 物理意义 ：
   
   - 等势线的对称性直接反映了电荷分布的对称性
   - 电场线始终与等势线垂直，从高电势指向低电势
   - 中心处电场为零，是电势的局部极大值点
5. 远处行为 ：
   
   - 当距离远大于圆环半径时，等势线趋近于球形，表现出点电荷的电势特征
   - 这验证了“远处的圆环相当于点电荷”的物理结论

---

## 5. Bonus：方板引力场（30分）

- 你实现的二维高斯积分方案：  
- 参数设置（L, M_plate, n）：  
- 结果表：

| z (m) | Fz (N) |
|---:|---:|
| 0.2 |  |
| 1.0 |  |
| 5.0 |  |
| 10.0 |  |

- 你对近场/远场行为的解释：  

---

## 6. AI 代码审查记录（必填）

- 你使用的关键 Prompt：  
- AI 输出中你识别出的错误或不严谨点（至少 2 条）：  
- 你的修正依据（数值分析 or 物理约束）：  
